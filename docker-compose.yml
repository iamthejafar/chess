# No "version:" field — it's obsolete in Compose v2+
services:
  backend:
    image: ghcr.io/iamthejafar/chess-backend:latest
    container_name: chess-backend
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env